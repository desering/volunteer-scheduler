# PostgreSQL Cheat Sheet

List all databases: `\l`

Connect to a different database: `\connect <dbname>`

List all tables and sequences: `\d`

List all tables: `\dt`

Describe table: `\d <tablename>`

To interact with tables, use standard SQL (`SELECT`, `INSERT INTO`, `UPDATE`, `DELETE`).
