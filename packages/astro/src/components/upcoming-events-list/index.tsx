import type {BoxProps} from "../../../styled-system/jsx";
import {For} from "solid-js";

export const UpcomingEventsList = (props: Props & BoxProps) => {
    return (
        <For each={['a']}>
            lalala
        </For>
    )
};
